<%= form_with(model: budget_transaction) do |form| %>
  <% if budget_transaction.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(budget_transaction.errors.count, "error") %> prohibited this budget_transaction from being saved:</h2>

      <ul>
        <% budget_transaction.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
